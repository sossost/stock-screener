import { notFound } from "next/navigation";

// Turn-Around 스크리너는 추후 리뉴얼 예정
// 핵심 로직은 /api/screener/turned-profitable에 보존되어 있음
const TurnAroundPage = () => {
  notFound();
};

export default TurnAroundPage;
