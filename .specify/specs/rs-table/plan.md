# Plan: RS 컬럼 + 테이블 정렬

1) 스키마 확장: 가격 메트릭 테이블에 `rs_score INT NULL` 컬럼 추가 및 마이그레이션 작성/검증.
2) ETL 확장: 일일 가격 ETL 후 12M 수익률 퍼센타일 계산 → `rs_score` 저장 로직 추가; 로깅(계산/스킵/에러) 포함.
3) 백필 명령: 최근 1년 기준일 반복 계산 커맨드(`yarn etl:rs-backfill`) 추가.
4) API 확장: 스크리너 API 응답에 `rs_score` 포함, N+1 없이 단일 쿼리 확인.
5) UI 업데이트: 테이블에 RS 컬럼 추가, 정렬(헤더 클릭 오름/내림 토글) 적용 및 아이콘 표시.
6) 검증: 마이그레이션/ETL 실행으로 RS 채워짐 확인, 테이블에서 RS 노출 및 정렬 동작 확인.
